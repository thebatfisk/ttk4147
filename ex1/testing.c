#include <unistd.h>
#include "time_utils.h"

int main()
{

    /* Task A */
    // struct timespec t = {1};

    // sleep(1);

    // busy_wait(t);

    // busy_wait_new(100);

    /* Task B */
    // estimate_ltcy();
    // est_resolution();

    /* Task C */
    context_switch();
}